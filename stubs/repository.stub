<?php

declare(strict_types=1);

namespace {{ namespace }};

use AndyDefer\Repository\AbstractRepository;
use AndyDefer\Records\Recordable;
use App\Models\{{ model_class }};
use Illuminate\Database\Eloquent\Builder;

final class {{ class }} extends AbstractRepository
{
    public function __construct()
    {
        // TODO: Replace {{ model_class }}::class with your actual model class
        // TODO: Replace {{ record_class }}::class with your actual record class
        parent::__construct({{ model_class }}::class, {{ record_class }}::class);
    }

    protected function applyFilters(Builder $query, Recordable $filters): void
    {
        // TODO: Check if filters is an instance of your filters class
        // TODO: Implement your filter logic here
        // Example:
        // if (property_exists($filters, 'name') && $filters->name !== null) {
        //     $query->where('name', 'like', '%' . $filters->name . '%');
        // }
    }
}